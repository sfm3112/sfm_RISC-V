# Engineering Log: June 9, 2026
**Author:** Stephen Meyer  
**Project:** Custom 32-Bit RISC-V Core (RV32I Variant)

## Target Goals for Today
1. Determine an efficient way to implement unsigned condition checks
2. Finish the rest of MC2
2. Ensure local parameters are correct
3. Begin synthesis & bug fixes

## BLTU and BGEU
* One idea to do unsigned comparisons would be to look at the carry bit instead of the negative bit
* This would work if the 32-bit number is actually 33-bit, where the MSB of each number is always zero, thus maintaining the unsigned component
* Then the old carry bit is now a sign bit, correctly denoting positive and negative numbers
* Branch less than unsigned will trigger when the carry bit is zero, and Branch greater than or equal to unsigned will trigger if the carry bit is 1

## MC2 AUIPC and JAL
* AUIPC needs to write MARout back to the registers
* JAL loads the new instruction address to PCcounter, and writes the current program count to a registers

## Initial synthesis
1. First synthesis:
	* In the CU, IW used in both input and WB_SEL signal vector
		- The IW select isn't actually used, just added as a placeholder/possible debugging
	* Typo's using "if" when correct keyword should be "begin"
	* Missing endcase for the branch case statement
2. Second Synthesis:
	* Left over "Func7" name in the ALU
		- Replaced with input "add_sub"
		- Should work, but noted as possible source of error
3. Third Synthesis:
	* Passed in the 32-bit addSubRes instead of the 33-bit full FaddSubRes to read the carry and negative bits
		- needed to zero extend the arithmetic to capture the carry bit
4. Fourth Synthesis:
	* always_comb in the CU supposedly doesn't contain purely combinational logic
		- Issue resolved after adding missing results of default cases
5. Fifth Synthesis:
	* No errors, concerning warnings about truncated values in the CU
		- WBsel output was defined as 2-bit, but should have been 3-bit
6. Sixth Synthesis:
	* No errors, no concerning warnings
	
